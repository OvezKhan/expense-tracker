# Optional: Set this ONLY if your backend API is deployed on a separate URL (e.g. Render/Railway)
# For local dev, leave it empty so Vite proxies /api to http://localhost:5000 automatically
VITE_API_URL=https://your-backend-api.onrender.com
